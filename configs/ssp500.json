{
  "seed": 0,
  "batch_size": 128,
  "validation_batch_size": 128,
  "gradient_accumulation": 1,
  "epochs": 500,
  "optimizer": "fused_adamw",
  "learning_rate": 0.0003,
  "betas": [0.9, 0.95],
  "epsilon": 1e-8,
  "weight_decay": 0.05,
  "warmup_epochs": 6,
  "scheduler": "cosine",
  "minimum_learning_rate": 3e-6,
  "precision": "bf16",
  "drop_last": false,
  "gradient_clip_norm": 1.0,
  "validate_every_epochs": 1,
  "validation_split": "2016",
  "validation_samples": 1454,
  "validation_lead_hours": 6,
  "selection_metric": "mean_5var_normalized_rmse_6h",
  "selection_weight_source": "raw_model",
  "top_k": 3,
  "early_stopping": false,
  "ema": false,
  "num_workers": 4,
  "inherited_root": ".",
  "cache_train": "cache/era5/train",
  "cache_validation": "cache/era5/val",
  "variant": "full",
  "run_dir": "checkpoints/ssp500"
}
